<% bookCoverClass = locals.large ?'book-cover-large' : '' %>
<% bookGridClass = locals.large ? 'book-grid-large' :'' %>
<div class="book-grid <%= bookGridClass%>">
    <% books.forEach(book => { %>
        <div class="container">
                <div class="book-cell">
                    <div class="book-img">
                        <a href="/books/<%= book.id %>">
                            <img class="img-fluid mb-3 mb-lg-0  <%= bookCoverClass %>" src="<%= book.coverImagePath %>">
                        </a>
                    </div>
                </div>
        </div>
    <% }) %>
</div>